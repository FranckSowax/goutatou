# Roue v2 + notifications interactives — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Segments Perdu/Rejouez serveur, images de lots, expiration + rappel des gains, boutons interactifs Whapi, pré-validation numéros campagnes. Spec : `docs/superpowers/specs/2026-07-11-roue-v2-design.md` (contrats exacts dedans).

## Global Constraints

- Tirage 100 % serveur. spin_wheel v2 re-déclarée INTÉGRALEMENT depuis 0011 (lire d'abord), ACL service_role re-révoquée/re-grantée, verrou advisory jti + anti-survente stock conservés à l'identique. RÉTROCOMPAT : weights 0/0 → byte-identique v1 (pgTAP).
- Jetons : retryToken jti = `${jti}:r1`, TTL 1 h, un jti déjà suffixé `:rN` ne génère JAMAIS de nouveau retry (anti-chaîne). Helper @goutatou/db/wheel étendu, testé.
- Bot : machine PURE intouchée. Workers pattern maison (poll try/catch/finally, log `[wheel-reminder] démarré`, single replica). Best-effort partout, opt-out respecté.
- Gates par paquet (db typecheck, web 109+, bot 108+, pgTAP 01-08), FR/tokens/light+dark. Branche `feature/roue-v2`.

---

### Task R1: Migration 0017 + spin_wheel v2 + pgTAP

**Files:** Create `supabase/migrations/20260711000017_roue_v2.sql`, `supabase/tests/database/08_spin_wheel_v2.test.sql`

- DDL (spec) : prizes.image_url ; restaurants.wheel_unlucky_weight/wheel_retry_weight (default 0, check >=0) ; wheel_spins.outcome ('prize'|'lose'|'retry', default 'prize') + prize_id nullable + expires_at + reminded_at ; bucket prize-media (copier le pattern policies de menu-photos/0004+0006).
- spin_wheel v2 : LIRE 0011 (version unbiased), re-déclarer intégralement ; tirage à seuil unique étendu aux 2 segments virtuels ; prize → + expires_at now()+30j ; lose/retry → ligne sans prize/code. ACL re-posée.
- pgTAP ≥10 asserts : rétrocompat 0/0 (résultat + lignes identiques v1), lose seul, retry seul, pondération (setseed), stock décrémenté prize seulement, jti unique toujours, expires_at posé, ACL anon/authenticated false.
- [ ] `supabase db reset` + suites 01-08 vertes. Commit `feat(db): roue v2 — segments perdu/rejouez, images, expiration (migration 0017)`.

---

### Task R2: Web — /roue v2, retry token, fidélité (réglages, images, expiration)

**Files:** Modify `packages/db/src/wheel.ts` (+ test : mintRetryToken — refuse jti contenant ':r'), `apps/web/src/app/api/roue/spin/route.ts`, `apps/web/src/app/roue/**` (repérer l'arbo réelle), `apps/web/src/app/app/fidelite/**` (actions + UI)

- Route spin : gérer outcomes ; retry → mintRetryToken (TTL 1h). AUCUNE décision de tirage côté client.
- /roue : segments Perdu/Rejouez (gris/ambre, tokens), images de lots (img segment, fallback label), résultats lose (FR sympa)/retry (bouton Rejouer → re-spin avec retryToken)/prize (+ date d'expiration affichée).
- /app/fidelite : réglages poids perdu/rejouez (0=off, action RLS pattern existant — écritures restaurants = client admin après garde, cf. 3A), upload image lot (action ≤4 Mo bucket prize-media, pattern photo menu), validation code : REFUS si expires_at < now() (message FR fixe), badge expiration dans la liste des gains si présente.
- [ ] Gate web complet. Commit `feat(web): roue v2 — perdu/rejouez, images de lots, expiration des gains`.

---

### Task R3: Bot — interactif Whapi, pré-validation campagnes, worker rappel

**Files:** Modify `packages/whapi` (+sendInteractiveUrl + test), `services/whatsapp/src/notifier.ts`, campagnes (worker/repo : pré-validation), Create `services/whatsapp/src/wheel/{repo,worker}.ts` + tests ; `config.ts` (+WHEEL_REMINDER_POLL_MS défaut 6h), `index.ts` (câblage)

- sendInteractiveUrl : POST /messages/interactive type button URL (vérifier le format exact dans le skill/docs whapi du repo `.agents/skills/whapi` si présent) ; notifier roue : interactif d'abord, catch → sendText actuel inchangé (fallback byte-identique au message v1).
- Pré-validation campagnes : helper pur `validPhoneForCountry(phone)` (241 + longueurs, testé) + whapi `checkContact(phone)` (GET /contacts/{phone} — vérifier endpoint) ; échec → recipient failed 'numéro invalide' sans envoi ni délai throttle.
- Worker wheel-reminder : poll, sélection gains outcome=prize, redeemed_at null, reminded_at null, expires_at <= now()+3j et > now(), client non opted_out → sendText rappel FR + reminded_at=now() (update AVANT envoi ? non : claim d'abord — update reminded_at where reminded_at is null returning, puis envoi best-effort, pattern claimDue statuts).
- [ ] Gate bot. Commit `feat(bot): boutons interactifs roue, pré-validation campagnes, rappel expiration gains`.

---

### Task R4: Revue finale opus + prod + smoke

- [ ] review-package base..HEAD → revue opus (spin_wheel v2 rétrocompat/ACL/verrous, jetons retry anti-chaîne, claim reminder, fallback interactif). Fix wave unique.
- [ ] Migration 0017 prod + notify pgrst + vérif ACL. Merge ff main + push (Netlify) + `railway up --detach --service whatsapp-bot`. Smoke SQL Chez Demo : poids perdu/rejouez posés, spin_wheel v2 appelée (service role) → outcomes cohérents, gain avec expires_at ; nettoyage. Ledger + mémoire.

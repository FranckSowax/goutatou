# Sondages & quiz WhatsApp — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Onglet Marketing « Sondages » : composer sondage/quiz → chaîne WhatsApp ou clients opt-in (worker throttlé). Spec : `docs/superpowers/specs/2026-07-12-sondages-design.md` (contrats exacts).

## Global Constraints

- Endpoints Whapi poll/quiz VÉRIFIÉS (doc skill / source whapi-mcp), confiance annoncée. Smoke prod SANS envoi à des tiers.
- Worker pattern maison (claim-first, throttle campagnes, mono-réplica, log `[poll-worker] démarré`). Textes FR, gating Pro.
- Gates par paquet (whapi 26+, bot 206+, web 121+, db + pgTAP). Branche `feature/sondages`.

---

### Task P1: Migration 0022 + whapi sendPoll/sendQuiz

**Files:** Create `supabase/migrations/20260712000022_polls.sql` (SQL exact de la spec) + `supabase/tests/database/10_polls.test.sql` (table, RLS activée, policy, check options/target/status — ≥5 asserts, pattern 06) ; Modify `packages/whapi/src/client.ts` + tests

- sendPoll(to, question, options) / sendQuiz(to, question, options, correctIndex) — payloads exacts vérifiés (POST /messages/poll et /messages/quiz ; champs title/options/count ? correct ? — source whapi-mcp générée).
- [ ] `supabase db reset` + suites 01-10 vertes, gate whapi. Commit `feat(whapi,db): sondages — table polls + envoi poll/quiz (migration 0022)`.

---

### Task P2: Bot — poll-worker (TDD)

**Files:** Create `services/whatsapp/src/polls/{repo.ts,worker.ts}` + tests ; Modify `config.ts` (+POLL_WORKER_POLL_MS défaut 30s), `index.ts`

- Repo : claimQueued (status queued + canal actif → update status 'sending' conditionnel returning), lecture wa_channel_id, clients opt-in (marketing_opt_in && !opted_out, chat_id), finish(pollId, {status, sentCount, error}).
- Worker : logique spec (channel → 1 envoi vers wa_channel_id ; optin → boucle throttlée, échecs comptés ; 0 client → failed FR ; quiz_correct non null → sendQuiz). TDD : channel ok, channel sans chaîne → failed FR, optin 3 clients dont 1 échec → sent 2 + partiel ? (spec : sent + sent_count=2 ; erreur seulement si 0 réussite — préciser : sent si ≥1 réussi, failed sinon), 0 opt-in → failed FR, quiz path, claim consomme.
- [ ] Gate bot. Commit `feat(bot): poll-worker — envoi sondages/quiz chaîne et clients opt-in`.

---

### Task P3: Web — onglet Sondages

**Files:** Create `apps/web/src/app/app/marketing/sondages/{page.tsx,composer.tsx,actions.ts}` ; Modify `apps/web/src/app/app/marketing/marketing-tabs.tsx` (5e onglet « Sondages »)

- Page (Pro-gated, pattern chaîne) : composer (spec — options dynamiques 2-12, switch quiz + bonne réponse, cible radio avec compteur opt-in live du resto, désactivée chaîne si pas de wa_channel_id avec aide FR) + historique (badges statut : queued « En file », sending « Envoi… », sent « Envoyé (N) », failed destructive + erreur).
- Action createPoll : garde membre, validation serveur (question non vide, 2-12 options non vides dédupliquées, quiz index dans les bornes, target valide), insert via client membre (RLS). Catchs FR fixes. AUCUN handler serveur→client.
- [ ] Gate web. Commit `feat(web): onglet Sondages — composer et historique`.

---

### Task P4: Revue + prod + smoke

- [ ] Revue inline contrôleur (scan handlers, validation serveur, claim worker). Migration 0022 prod + notify pgrst. Merge ff main + push + railway up. Smoke sans tiers : poll cible optin avec 0 client → failed + message FR, 0 envoi. Ledger + mémoire.
